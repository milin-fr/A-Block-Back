{% extends 'base.html.twig' %}

{% block title %}Hint{% endblock %}

{% block body %}
    <h1>Hint</h1>

    <table class="table">
        <tbody>
            <tr>
                <th>Id</th>
                <td>{{ hint.id }}</td>
            </tr>
            <tr>
                <th>Text</th>
                <td>{{ hint.text }}</td>
            </tr>
        </tbody>
    </table>

    <a href="{{ path('admin_hint_index') }}">back to list</a>

    <a href="{{ path('admin_hint_edit', {'id': hint.id}) }}">edit</a>

    {{ include('admin/hint/_delete_form.html.twig') }}
{% endblock %}
