{% extends 'base.html.twig' %}

{% block title %}New Hint{% endblock %}

{% block body %}
<div class="wrapper-entity-form">
    <table class="table table-striped table-bordered table-light text-center">
        <thead class="thead">
            <tr>
                <th>New Hint</th>
            </tr>
        </thead>
        <tbody>
            <tr>
                <td>    {{ include('admin/hint/_form.html.twig', {'button_label': 'Create'}) }}
                </td>
            </tr>
        </tbody>
    </table>
        <a href="{{ path('admin_hint_index') }}" button type="button" class="btn btn-primary btn-block">Back to list</a>
</div>
{% endblock %}
