{% extends 'base.html.twig' %}

{% block title %}Program{% endblock %}

{% block body %}
    <h1>Program</h1>

    <table class="table">
        <tbody>
            <tr>
                <th>Id</th>
                <td>{{ program.id }}</td>
            </tr>
            <tr>
                <th>Title</th>
                <td>{{ program.title }}</td>
            </tr>
            <tr>
                <th>Image</th>
                <td><img src="{{ asset('uploads/program_img/'~ program.imgPath) }}" alt="Program Image Abloc" style="width:50px;"></td>
            </tr>            
        </tbody>
    </table>
    <table class="table">
        <tbody>
            <td><a href="{{ path('admin_program_edit', {'id': program.id}) }}" class="btn btn-success btn-block">Edit Program</a></td>
            <td>{{ include('admin/program/_delete_form.html.twig') }}</td>
        </tbody>
    </table>
                <a href="{{ path('admin_program_index') }}" button type="button" class="btn btn-primary btn-block">Back to list</a>
{% endblock %}
