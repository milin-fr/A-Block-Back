{% extends 'base.html.twig' %}

{% block title %}New Program{% endblock %}

{% block body %}
<div class="wrapper-entity-form">
    <p class="d-flex justify-content-end">Edit Program</p>
    <table class="table table-striped table-dark">
        <thead class="thead">
            <tr>
                <th>Id</th>
                <th></th>
            </tr>
        </thead>
        <tbody>
            <tr>
                <td>{{ program.id }}</td>
                <td>    {{ include('admin/program/_form.html.twig', {'button_label': 'Create'}) }}
                </td>
            </tr>
        </tbody>
    </table>
        <a href="{{ path('admin_program_index') }}" button type="button" class="btn btn-primary btn-block">Back to list</a>
</div>
{% endblock %}