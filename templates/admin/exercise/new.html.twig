{% extends 'base.html.twig' %}
{% block title %}New Exercise{% endblock %}
{% form_theme form 'bootstrap_4_layout.html.twig' %}
{% block body %}
<div class="wrapper-entity-form">
    <p class="d-flex justify-content-end">Create New Exercise</p>
    <table class="table table-striped table-dark">
        <thead class="thead">
            <tr>
                <th>Id</th>
                <th></th>
            </tr>
        </thead>
        <tbody>
            <tr>
                <td>{{ exercise.id }}</td>
                <td>    {{ include('admin/exercise/_form.html.twig', {'button_label': 'Create'}) }}
                </td>
            </tr>
        </tbody>
    </table>
        <a href="{{ path('admin_exercise_index') }}" button type="button" class="btn btn-primary btn-block">Back to list</a>
</div>
{% endblock %}
