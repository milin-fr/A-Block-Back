{% extends 'base.html.twig' %}

{% block title %}Edit Exercise{% endblock %}

{% block body %}
<div class="wrapper-entity-form">
    <p class="d-flex justify-content-end">Edit Exercise</p>
    <table class="table table-striped table-dark">
        <thead class="thead">
            <tr>
                <th>Id</th>
                <th></th>
            </tr>
        </thead>
        <tbody>
            <tr>
                <td>{{ exercise.id }}</td>
                <td>    {{ include('admin/exercise/_form.html.twig', {'button_label': 'Update'}) }}
                        {{ include('admin/exercise/_delete_form.html.twig') }}
                </td>
            </tr>
        </tbody>
    </table>
        <a href="{{ path('admin_exercise_index') }}" button type="button" class="btn btn-primary btn-block">Back to list</a>
</div>
{% endblock %}
