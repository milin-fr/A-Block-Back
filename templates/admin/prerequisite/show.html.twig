{% extends 'base.html.twig' %}

{% block title %}Prerequisite{% endblock %}

{% block body %}
    <h1>Prerequisite</h1>

<div class="wrapper">
  <div class="table">
    <div class="row header">
      <div class="cell">
        Id
      </div>
      <div class="cell">
        Description
      </div>
      <div class="cell">
        Actions
      </div>
    </div>
    <div class="row">
      <div class="cell" data-title="Id">
        {{ prerequisite.id }}
      </div>
      <div class="cell" data-title="Description">
        {{ prerequisite.description }}
      </div>
      <div class="cell" data-title="Actions">
          <a href="{{ path('admin_prerequisite_edit', {'id': prerequisite.id}) }}" class="btn btn-success btn-block">Edit Prerequisite</a>
        {{ include('admin/prerequisite/_delete_form.html.twig') }}
      </div>
    </div>
  </div>
<a href="{{ path('admin_prerequisite_index') }}" button type="button" class="btn btn-primary btn-block">Back to list</a>
{% endblock %}
