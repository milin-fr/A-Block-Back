{% extends 'base.html.twig' %}

{% block title %}Prerequisite{% endblock %}

{% block body %}
    <h1>Prerequisite</h1>

    <table class="table">
        <tbody>
            <tr>
                <th>Id</th>
                <td>{{ prerequisite.id }}</td>
            </tr>
            <tr>
                <th>Description</th>
                <td>{{ prerequisite.description }}</td>
            </tr>
        </tbody>
    </table>

    <a href="{{ path('admin_prerequisite_index') }}">back to list</a>

    <a href="{{ path('admin_prerequisite_edit', {'id': prerequisite.id}) }}">edit</a>

    {{ include('admin/prerequisite/_delete_form.html.twig') }}
{% endblock %}
