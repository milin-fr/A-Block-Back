{% extends 'base.html.twig' %}

{% block title %}Prerequisite{% endblock %}

{% block body %}
    <h1>Prerequisite</h1>

    <table class="table">
        <tbody>
            <tr>
                <th>Id</th>
                <td>{{ prerequisite.id }}</td>
            </tr>
            <tr>
                <th>Description</th>
                <td>{{ prerequisite.description }}</td>
            </tr>
        </tbody>
    </table>
    <table class="table">
        <tbody>
            <td><a href="{{ path('admin_prerequisite_edit', {'id': prerequisite.id}) }}" class="btn btn-success btn-block">Edit Prerequisite</a></td>
            <td>{{ include('admin/prerequisite/_delete_form.html.twig') }}</td>
        </tbody>
    </table>
                <a href="{{ path('admin_prerequisite_index') }}" button type="button" class="btn btn-primary btn-block">Back to list</a>
{% endblock %}
