{% extends 'base.html.twig' %}

{% block title %}New Prerequisite{% endblock %}

{% block body %}
<div class="wrapper-entity-form">
    <p class="d-flex justify-content-end">Create New Prerequisite</p>
    <table class="table table-striped table-dark">
        <thead class="thead">
            <tr>
                <th>Id</th>
                <th></th>
            </tr>
        </thead>
        <tbody>
            <tr>
                <td>{{ prerequisite.id }}</td>
                <td>    {{ include('admin/prerequisite/_form.html.twig', {'button_label': 'Create'}) }}
                </td>
            </tr>
        </tbody>
    </table>
        <a href="{{ path('admin_prerequisite_index') }}" button type="button" class="btn btn-primary btn-block">Back to list</a>
</div>
{% endblock %}
