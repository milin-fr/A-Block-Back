{% extends 'base.html.twig' %}

{% block title %}New Prerequisite{% endblock %}

{% block body %}
<div class="info-container">
    <div class="info-body">
    <table class="table table-light text-center">
        <thead class="info-thead">
            <tr>
                <th>New Prerequisite</th>
            </tr>
        </thead>
        <tbody>
            <tr>
                <td>{{ include('admin/prerequisite/_form.html.twig', {'button_label': 'Create'}) }}</td>
            </tr>
        </tbody>
    </table>
        <div class="back-list-button">
            <a href="{{ path('admin_prerequisite_index') }}" button type="button" class="btn btn-primary btn-block">Back to list</a>
        </div>
    </div>
</div>
{% endblock %}
