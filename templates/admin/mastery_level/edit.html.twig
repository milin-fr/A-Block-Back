{% extends 'base.html.twig' %}

{% block title %}Edit MasteryLevel{% endblock %}

{% block body %}
<div class="info-container">
    <div class="info-body">
    <p class="route d-flex justify-content-end">Edit Mastery Level</p>
    <table class="table table-bordered table-light text-center">
        <thead class="info-thead">
            <tr>
                <th>ID : {{ mastery_level.id }}</th>
            </tr>
        </thead>
        <tbody>
            <tr>
                <td>    {{ include('admin/mastery_level/_form.html.twig', {'button_label': 'Update'}) }}
                        {{ include('admin/mastery_level/_delete_form.html.twig') }}
                </td>
            </tr>
        </tbody>
    </table>
        <a href="{{ path('admin_mastery_level_index') }}" button type="button" class="btn btn-primary btn-block">Back to list</a>
</div>
{% endblock %}
