{% extends 'base.html.twig' %}

{% block title %}MasteryLevel{% endblock %}

{% block body %}
    <h1>MasteryLevel</h1>

    <table class="table">
        <tbody>
            <tr>
                <th>Id</th>
                <td>{{ mastery_level.id }}</td>
            </tr>
            <tr>
                <th>Title</th>
                <td>{{ mastery_level.title }}</td>
            </tr>
        </tbody>
    </table>
    <table class="table">
        <tbody>
            <td><a href="{{ path('admin_mastery_level_edit', {'id': mastery_level.id}) }}" class="btn btn-success btn-block">Edit Mastery Level</a></td>
            <td>{{ include('admin/mastery_level/_delete_form.html.twig') }}</td>
        </tbody>
    </table>
                <a href="{{ path('admin_mastery_level_index') }}" button type="button" class="btn btn-primary btn-block">Back to list</a>
{% endblock %}
