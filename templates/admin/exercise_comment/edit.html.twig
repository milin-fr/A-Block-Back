{% extends 'base.html.twig' %}

{% block title %}Edit ExerciseComment{% endblock %}

{% block body %}
<div class="wrapper-entity-form">
    <p class="d-flex justify-content-end">Edit Exercise Comment</p>
    <table class="table table-striped table-dark">
        <thead class="thead">
            <tr>
                <th>ID</th>
                <th></th>
            </tr>
        </thead>
        <tbody>     
            <tr>
                <td>{{ exercise_comment.id }}</td>
                <td>    {{ include('admin/exercise_comment/_form.html.twig', {'button_label': 'Update'}) }}
                        {{ include('admin/exercise_comment/_delete_form.html.twig') }}
                </td>
            </tr>
        </tbody>
    </table>
        <a href="{{ path('admin_exercise_comment_index') }}" button type="button" class="btn btn-primary btn-block">Back to list</a>
</div>
{% endblock %}
