{% extends 'base.html.twig' %}

{% block title %}ExerciseComment{% endblock %}

{% block body %}
    <h1>ExerciseComment</h1>

    <table class="table">
        <tbody>
            <tr>
                <th>Id</th>
                <td>{{ exercise_comment.id }}</td>
            </tr>
            <tr>
                <th>Text</th>
                <td>{{ exercise_comment.text }}</td>
            </tr>
        </tbody>
    </table>

    <a href="{{ path('exercise_comment_index') }}">back to list</a>

    <a href="{{ path('exercise_comment_edit', {'id': exercise_comment.id}) }}">edit</a>

    {{ include('exercise_comment/_delete_form.html.twig') }}
{% endblock %}
