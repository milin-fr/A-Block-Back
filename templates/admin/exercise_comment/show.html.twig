{% extends 'base.html.twig' %}

{% block title %}ExerciseComment{% endblock %}

{% block body %}
    <h1>ExerciseComment</h1>

    <table class="table">
        <tbody>
            <tr>
                <th>Id</th>
                <td>{{ exercise_comment.id }}</td>
            </tr>
            <tr>
                <th>Text</th>
                <td>{{ exercise_comment.text }}</td>
            </tr>
        </tbody>
    </table>
    <table class="table">
        <tbody>
            <td><a href="{{ path('admin_exercise_comment_edit', {'id': exercise_comment.id}) }}" class="btn btn-success btn-block">Edit Comment</a></td>
            <td>{{ include('admin/exercise_comment/_delete_form.html.twig') }}</td>
        </tbody>
    </table>
{% endblock %}
