{% extends 'base.html.twig' %}

{% block title %}ExerciseComment{% endblock %}

{% block body %}
    <h1>ExerciseComment</h1>

    </table><div class="wrapper">
  <div class="table">
    <div class="row header">
      <div class="cell">
        Id
      </div>
      <div class="cell">
        Text
      </div>
      <div class="cell">
        Actions
      </div>
    </div>
    <div class="row">
      <div class="cell" data-title="Id">
        {{ exercise_comment.id }}
      </div>
      <div class="cell" data-title="Text">
        {{ exercise_comment.text }}
      </div>
      <div class="cell" data-title="Actions">
          <a href="{{ path('admin_exercise_comment_edit', {'id': exercise_comment.id}) }}" class="btn btn-success btn-block">Edit Comment</a>
        {{ include('admin/exercise_comment/_delete_form.html.twig') }}
      </div>
    </div>
  </div>
<a href="{{ path('admin_exercise_comment_index') }}" button type="button" class="btn btn-primary btn-block">Back to list</a>
{% endblock %}
