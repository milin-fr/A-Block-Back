{% extends 'base.html.twig' %}

{% block title %}ProgramComment{% endblock %}

{% block body %}
    <h1>ProgramComment</h1>

    <table class="table">
        <tbody>
            <tr>
                <th>Id</th>
                <td>{{ program_comment.id }}</td>
            </tr>
            <tr>
                <th>Text</th>
                <td>{{ program_comment.text }}</td>
            </tr>
        </tbody>
    </table>
    <table class="table">
        <tbody>
            <td><a href="{{ path('admin_program_comment_edit', {'id': program_comment.id}) }}" class="btn btn-success btn-block">Edit Comment</a></td>
            <td>{{ include('admin/program_comment/_delete_form.html.twig') }}</td>
        </tbody>
    </table>
                <a href="{{ path('admin_program_comment_index') }}" button type="button" class="btn btn-primary btn-block">Back to list</a>
{% endblock %}
