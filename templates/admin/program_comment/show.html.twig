{% extends 'base.html.twig' %}

{% block title %}ProgramComment{% endblock %}

{% block body %}
    <h1>ProgramComment</h1>

    </table><div class="wrapper">
  <div class="table">
    <div class="row header">
      <div class="cell">
        Id
      </div>
      <div class="cell">
        Text
      </div>
      <div class="cell">
        Actions
      </div>
    </div>
    <div class="row">
      <div class="cell" data-title="Id">
        {{ program_comment.id }}
      </div>
      <div class="cell" data-title="Text">
        {{ program_comment.text }}
      </div>
      <div class="cell" data-title="Actions">
          <a href="{{ path('admin_program_comment_edit', {'id': program_comment.id}) }}" class="btn btn-success btn-block">Edit Comment</a>
        {{ include('admin/program_comment/_delete_form.html.twig') }}
      </div>
    </div>
  </div>
<a href="{{ path('admin_program_comment_index') }}" button type="button" class="btn btn-primary btn-block">Back to list</a>
{% endblock %}
