{% extends 'base.html.twig' %}

{% block title %}Edit ProgramComment{% endblock %}

{% block body %}
<div class="wrapper-entity-form">
    <p class="d-flex justify-content-end">Edit Program Comment</p>
    <table class="table table-striped table-bordered table-light text-center">
        <thead class="thead">
            <tr>
                <th>ID</th>
                <th></th>
            </tr>
        </thead>
        <tbody>
            <tr>
                <td>{{ program_comment.id }}</td>
                <td>    {{ include('admin/program_comment/_form.html.twig', {'button_label': 'Update'}) }}
                        {{ include('admin/program_comment/_delete_form.html.twig') }}
                </td>
            </tr>
        </tbody>
    </table>
        <a href="{{ path('admin_program_comment_index') }}" button type="button" class="btn btn-primary btn-block">Back to list</a>
</div>
{% endblock %}
