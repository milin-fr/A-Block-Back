<form method="post" action="{{ path('program_comment_delete', {'id': program_comment.id}) }}" onsubmit="return confirm('Are you sure you want to delete this item?');">
    <input type="hidden" name="_method" value="DELETE">
    <input type="hidden" name="_token" value="{{ csrf_token('delete' ~ program_comment.id) }}">
    <button class="btn">Delete</button>
</form>
