{% extends 'base.html.twig' %}

{% block title %}New User{% endblock %}

{% block body %}
<div class="info-container">
    <div class="info-body">
    <table class="table table-light text-center">
        <thead class="info-thead">
            <tr>
                <th>New User</th>
            </tr>
        </thead>
        <tbody>
            <tr>
                <td>{{ include('admin/user/_form.html.twig', {'button_label': 'Create'}) }}</td>
            </tr>
        </tbody>
    </table>
        <div class="back-list-button">
            <a href="{{ path('admin_user_index') }}" button type="button" class="btn btn-primary btn-block">Back to list</a>
        </div>
    </div>
</div>
{% endblock %}
