{% extends 'base.html.twig' %}

{% block title %}Edit User{% endblock %}

{% block body %}
<div class="info-container">
    <div class="info-body">
    <p class="route d-flex justify-content-end">Edit User</p>
    <table class="table table-bordered table-light text-center">
        <thead class="info-thead">
            <tr>
                <th>ID : {{ user.id }}</th>
            </tr>
        </thead>
        <tbody>
            <tr>
                <td>    {{ include('admin/user/_form.html.twig', {'button_label': 'Update'}) }}
                        {{ include('admin/user/_delete_form.html.twig') }}
                </td>
            </tr>
        </tbody>
    </table>
        <a href="{{ path('admin_user_index') }}" button type="button" class="btn btn-primary btn-block">Back to list</a>
</div>
{% endblock %}
