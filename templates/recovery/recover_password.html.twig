{% extends 'base.html.twig' %}
{% block header %}

{% endblock %}

{% block title %}Recuperer le mot de passe{% endblock %}

{% block body %}
<div class="wrapper-entity-form">
<h1>Password Recovery</h1>
    <p class="d-flex justify-content-end">Password recovery</p>
    {{ form_start(form) }}
    <table class="table table-striped table-dark">
        <thead class="thead">
            <tr>
                <th>Type your Email</th>
            </tr>
        </thead>
        <tbody>
            <tr>
                <td>{{ form_widget(form.email) }}</td>
            </tr>
        </tbody>
    {{ form_end(form) }}
        </table>
        <button type="submit" class="btn btn-success btn-block">Recover</button>
        <a href="{{ path('admin_user_index') }}" button type="button" class="btn btn-primary btn-block">Back to list</a>
</div>
{% endblock %}
