{% extends 'base.html.twig' %}

{% block title %}Hint{% endblock %}

{% block body %}
    <h1>Hint</h1>

    <table class="table">
        <tbody>
            <tr>
                <th>Id</th>
                <td>{{ hint.id }}</td>
            </tr>
            <tr>
                <th>Text</th>
                <td>{{ hint.text }}</td>
            </tr>
            <tr>
                <th>Created_at</th>
                <td>{{ hint.createdAt ? hint.createdAt|date('Y-m-d H:i:s') : '' }}</td>
            </tr>
            <tr>
                <th>Updated_at</th>
                <td>{{ hint.updatedAt ? hint.updatedAt|date('Y-m-d H:i:s') : '' }}</td>
            </tr>
        </tbody>
    </table>

    <a href="{{ path('hint_index') }}">back to list</a>

    <a href="{{ path('hint_edit', {'id': hint.id}) }}">edit</a>

    {{ include('hint/_delete_form.html.twig') }}
{% endblock %}
